name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.9", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[dev]"
      - name: Byte-compile every file (catches truncated sources)
        run: python -m compileall -q tokenviz tests
      - name: Tests
        run: python -m pytest -v
      - name: Run the installed command
        shell: bash
        run: |
          tokenviz --version
          tokenviz "hello world" | grep -q "Total tokens: 2"
          printf 'a\nb c d\n' | tokenviz --top 1 | grep -q "Total lines analyzed: 1"
